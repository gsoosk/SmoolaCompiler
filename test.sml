class b {

    def main() : int {

        return 2;
    }
}
class c extends b{

    def test2(x1 : int) : int {

        return 2;
    }
}
class a extends c {

    def test() : int {
        var i : int;
        var y : int;
        var b1 : int;
        var b2 : boolean;
        var x : int;
        var m : c;
        var arr : int[];
        var l : int;
        i = (x + y + b1) ;

        while(x == ( x + y + x + 2 * x / i ) ){
             b1 = true;
        }
        while(x && ( x + y + x + 2 * x / i ) ){
             b1 = true;
        }
        if(x && ( x + y + x + 2 * x / i ) ) then {
             b1 = true;
        }

        l = (m).test2(b1);
        l = arr.length;

        return x;
    }
}
