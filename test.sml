class b {

    def main() : int {

        return 2;
    }
}
class c extends b{

    def test2() : int {

        return 2;
    }
}
class a extends c {

    def test() : int {
        var i : int;
        var y : int;
        var b1 : int;
        var b2 : boolean;
        var x : int;
        var m : c;
        i = (x + y + b1) ;

        while(x == ( x + y + x + 2 * x / i ) ){
             b1 = true;
        }
        while(x && ( x + y + x + 2 * x / i ) ){
             b1 = true;
        }
        if(x && ( x + y + x + 2 * x / i ) ) then {
             b1 = true;
        }

        l = (m).main(3+2);


        return x;
    }
}
