class b {

    def main() : int {

        return 2;
    }
}
class c {

    def test2() : int {

        return 2;
    }
}
class a extends c {

    def test() : int {
        var i : int;
        var y : int;
        var b1 : int;
        var b2 : boolean;
        var x : int;
<<<<<<< HEAD
        i = x + y;

        writeln(this);
=======
        i = (x + y + b1) ;

        while(x == ( x + y + x + 2 * x / i ) ){
             b1 = true;
        }
        while(x && ( x + y + x + 2 * x / i ) ){
             b1 = true;
        }
        if(x && ( x + y + x + 2 * x / i ) ) then {
             b1 = true;
        }

        l = (2+3).a(3+2);


>>>>>>> phase3-farzad
        return x;
    }
}
